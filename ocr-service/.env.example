# Secret key used by the OCR service. In CI set `OCR_API_KEY` as a repository secret; locally replace the value below.
OCR_API_KEY=REPLACE_WITH_OCR_API_KEY
S3_BUCKET_NAME=your-bucket-name-here
# AWS credentials should be handled via IAM roles in production
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
