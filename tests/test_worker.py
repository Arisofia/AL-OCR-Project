"""Unit tests for the AL OCR worker process."""

def test_worker_initialization():
    """Verify that the worker can be initialized correctly."""
    worker_initialized = True
    assert worker_initialized is True
