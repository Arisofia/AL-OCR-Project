---
name: Retroactive PR Assignment
on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: "Pull request number to assign reviewers to"
        required: true
        type: string
jobs:
  assign-reviewers:
    runs-on: ubuntu-22.04
    steps:
      - name: Assign reviewers
        uses: actions/github-script@f28e40c453d6c6c308a0d8e8a559c3f3f05f34cd # v8.0.0
        with:
          script: |
            const prNumber = parseInt(core.getInput('pr_number'));
            const reviewers = ['Arisofia', 'jenineferderas'];
            await github.rest.pulls.requestReviewers({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
              reviewers
            });
