# Frontend development environment example
# Copy this file to .env.development and update values as needed.

# Base URL for the OCR API used by the frontend during development
VITE_API_BASE=http://localhost:8000

# API key for the OCR API. In development you can use the default below.
# In production, set this to a secure value via your host/provider secrets.
VITE_API_KEY=default_secret_key
