# Frontend development environment example
# Copy this file to .env.development and update values as needed.

# Base URL for the OCR API used by the frontend during development
VITE_API_BASE=http://localhost:8000

# API key for the OCR API. For local dev set a value (e.g. REPLACE_WITH_STAGING_API_KEY).
# For CI, store the staging key as a GitHub Actions secret named `STAGING_API_KEY` and the workflows will use it.
VITE_API_KEY=REPLACE_WITH_STAGING_API_KEY
